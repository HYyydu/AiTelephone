{
  "name": "aicustomer-call-root",
  "private": true,
  "description": "Root package for Railway; build/start delegate to backend.",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
